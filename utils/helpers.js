module.exports = {
    // The custom method 'selectExercise' is used to select the exercise.
    selectExercise: (data) => {

    },
    // Used to add all the exercises selected and add it to the desired routine.
    addToRoutine: (data) => {

    }
}
// Or I can use a post method to obtain all the information I need to create a routine. <<< Easier option.
// Need to create a form.