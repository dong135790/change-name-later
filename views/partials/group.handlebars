<section class="groupContainer col-6 col-sm-3">
    <a href="/group/{{id}}">
    <p>{{name}}</p>
    <img src="images/{{image}}" alt="{{description}}" width="300" height="300">
    </a>
</section>
{{#if nextLine}}
       <div class="w-100"></div>
{{/if}}