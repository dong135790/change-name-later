// Require the modules from same directory (folder)
const User = require('./Excercise');
const User = require('./User');

const Exercise = require('./Exercise');
const User = require('./User');



module.exports = { Exercise, User }
